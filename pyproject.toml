[tool.poetry]
name = "triflow"
version = "0.5.0"
description = "Automatic finite difference discretization for 1D PDE with fast temporal solvers."

license = "MIT"

authors = [
    "Nicolas Cellier <contact@nicolas-cellier.net>"
]

readme = 'README.md'  # Markdown files are supported

repository = "http://github.com/locie/triflow"
documentation = "https://locie.github.io/triflow"

keywords = ['PDE', 'finite-difference', 'physic',
            'modelling', 'interactive']

[tool.poetry.dependencies]
python = ">=3.5"

# core computation
numpy = "^1"
scipy = "^1"
sympy = "^1"
theano = "^1"

# pipeline for data and display
streamz = "^0.3"

# data container:
xarray = "^0.10"
dask = "^0.17.2"

# path management
"path.py" = "^11.0"

# iteration display
tqdm = "^4.23"

# better serialization
cloudpickle = "^0.5.2"

# dataclass for python < 3.7
attrs = "^18.1"

# needed for decision-tree building
sklearn = "^0"

# allow dynamic function signature
python-forge = "^18"

# interactive work with jupyter
jupyter = {version = "^1.0", extras = ["jupyter"]}
ipywidgets = {version = "^7.2", extras = ["jupyter"]}
bokeh = {version = "^0.13.0", extras = ["jupyter"]}
holoviews = {version = "^1.10", extras = ["jupyter"]}

[tool.poetry.dev-dependencies]
# linting & formatting
pylama = "^7.4"
isort = "^4.3"

# tests
pytest = "^3.5"

# doc generation
sphinx = "^1.7"
matplotlib = "^2.2"

# coverage
python-coveralls = "^2.9"
pytest-cov = "^2.5"

